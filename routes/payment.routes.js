const express = require('express');
const subRouter = express.Router();

// Payment endpoints are added in a follow-up step.
// This file exists to fix router wiring so the API boots.

module.exports = subRouter;

